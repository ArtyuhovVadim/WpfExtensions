<Window
    x:Class="DemoApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:adp="clr-namespace:WpfExtensions.AttachedDependencyProperties;assembly=WpfExtensions"
    xmlns:con="clr-namespace:WpfExtensions.Controls;assembly=WpfExtensions"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DemoApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:parts="clr-namespace:WpfExtensions.Controls.ColorPicker.Parts;assembly=WpfExtensions"
    Title="DemoApp"
    Width="1280"
    Height="720"
    mc:Ignorable="d">

    <DockPanel>
        <TabControl TabStripPlacement="Left">
            <TabItem Header="ColorPicker">
                <Grid adp:GridProperties.ColumnDefinitionsEx="* 5*">
                    <GroupBox Header="Properties">
                        <GroupBox.Resources>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="HorizontalAlignment" Value="Right" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="Margin" Value="0,3,5,3" />
                            </Style>
                            <Style TargetType="{x:Type TextBox}">
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="Margin" Value="0,3,0,3" />
                            </Style>
                        </GroupBox.Resources>
                        <con:StackPanelEx>
                            <GroupBox Grid.IsSharedSizeScope="True" Header="ComplexColor">
                                <con:StackPanelEx>
                                    <Grid adp:GridProperties.ColumnDefinitionsEx="auto:PropertyGroup *">
                                        <TextBlock Grid.Column="0" Text="Red" />
                                        <TextBox Grid.Column="1" Text="{Binding ElementName=HsvColorPicker, Path=ComplexColor.Red}" />
                                    </Grid>
                                    <Grid adp:GridProperties.ColumnDefinitionsEx="auto:PropertyGroup *">
                                        <TextBlock Grid.Column="0" Text="Green" />
                                        <TextBox Grid.Column="1" Text="{Binding ElementName=HsvColorPicker, Path=ComplexColor.Green}" />
                                    </Grid>
                                    <Grid adp:GridProperties.ColumnDefinitionsEx="auto:PropertyGroup *">
                                        <TextBlock Grid.Column="0" Text="Blue" />
                                        <TextBox Grid.Column="1" Text="{Binding ElementName=HsvColorPicker, Path=ComplexColor.Blue}" />
                                    </Grid>
                                    <Grid adp:GridProperties.ColumnDefinitionsEx="auto:PropertyGroup *">
                                        <TextBlock Grid.Column="0" Text="Alpha" />
                                        <TextBox Grid.Column="1" Text="{Binding ElementName=HsvColorPicker, Path=ComplexColor.Alpha}" />
                                    </Grid>
                                    <Grid adp:GridProperties.ColumnDefinitionsEx="auto:PropertyGroup *">
                                        <TextBlock Grid.Column="0" Text="Hue" />
                                        <TextBox Grid.Column="1" Text="{Binding ElementName=HsvColorPicker, Path=ComplexColor.Hue, StringFormat='0.000'}" />
                                    </Grid>
                                    <Grid adp:GridProperties.ColumnDefinitionsEx="auto:PropertyGroup *">
                                        <TextBlock Grid.Column="0" Text="Saturation" />
                                        <TextBox Grid.Column="1" Text="{Binding ElementName=HsvColorPicker, Path=ComplexColor.Saturation, StringFormat='0.000'}" />
                                    </Grid>
                                    <Grid adp:GridProperties.ColumnDefinitionsEx="auto:PropertyGroup *">
                                        <TextBlock Grid.Column="0" Text="Value" />
                                        <TextBox Grid.Column="1" Text="{Binding ElementName=HsvColorPicker, Path=ComplexColor.Value, StringFormat='0.000'}" />
                                    </Grid>
                                    <Grid adp:GridProperties.ColumnDefinitionsEx="auto:PropertyGroup * auto">
                                        <TextBlock Grid.Column="0" Text="Color" />
                                        <TextBox Grid.Column="1" Text="{Binding ElementName=HsvColorPicker, Path=ComplexColor.Color}" />
                                        <Border
                                            Grid.Column="2"
                                            Width="{Binding RelativeSource={RelativeSource Self},
                                                            Path=ActualHeight}"
                                            Margin="5,3,0,3"
                                            BorderBrush="Black"
                                            BorderThickness="1"
                                            SnapsToDevicePixels="True"
                                            UseLayoutRounding="True">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding ElementName=HsvColorPicker, Path=ComplexColor.Color}" />
                                            </Border.Background>
                                        </Border>
                                    </Grid>
                                </con:StackPanelEx>
                            </GroupBox>
                        </con:StackPanelEx>
                    </GroupBox>
                    <Grid
                        Grid.Column="1"
                        adp:GridProperties.ColumnDefinitionsEx="* * *"
                        adp:GridProperties.RowDefinitionsEx="* *">
                        <GroupBox
                            Grid.Row="0"
                            Grid.Column="0"
                            Padding="5"
                            Header="HsvColorPicker">
                            <parts:HsvColorPicker x:Name="HsvColorPicker" />
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
